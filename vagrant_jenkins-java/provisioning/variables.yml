---
# Project
symfony_version: "2.5.*"
project_root: "/var/www/{{ parameters.project_name }}"
project_web_root: "{{ project_root }}/web"
project_databases:
  - "{{ parameters.project_name }}"
  - "{{ parameters.project_name }}-dev"
  - "{{ parameters.project_name }}-tst"

# Composer
composer_download_url: https://getcomposer.org/composer.phar

#MySQL
mysql_port: 3306
mysql_root_db_pass: ""
